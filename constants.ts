
// All data is now loaded dynamically from Google Sheets.
// Constants kept empty to avoid breaking legacy imports if any.

export const INITIAL_CATEGORIES: string[] = [];
export const INITIAL_MENU_ITEMS: any[] = [];
